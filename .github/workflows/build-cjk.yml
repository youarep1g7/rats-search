name: Build Windows x64 (CJK)

on:
  workflow_dispatch:
  push:
    branches: [ cjk-search ]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repo with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Qt 6.9.1 (mingw_64) + WebSockets
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.9.1'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_mingw'
          modules: 'qtwebsockets'
          cache: true

      - name: Setup CMake & Ninja
        uses: lukka/get-cmake@latest

      - name: Configure
        run: cmake -B build -G "Ninja" -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH="$env:Qt6_DIR/../../.." -DRATS_SEARCH_BUILD_TESTS=OFF
        shell: pwsh

      - name: Build
        run: cmake --build build --config Release
        shell: pwsh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: RatsSearch-Windows-x64-CJK
          path: build/bin/
